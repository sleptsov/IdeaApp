export enum DATA {
    TODOS = 'todos'
}

export enum SORT_TYPES {
    CREATED = 'CreatedOn',
    MODIFIED = 'ModifiedOn',
    QUEUEING = 'Queueing'
}

export enum TODOS_VIEW_STATUS {
    ALL = 'All',
    DONE = 'Complete',
    NOTDONE = 'UnComplete'
}